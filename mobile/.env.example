# Copie para .env e ajuste pro IP de LAN do PC rodando `python manage.py runserver`
# (nao use "localhost" - no celular isso aponta pro proprio celular, nao pro PC)
EXPO_PUBLIC_API_BASE_URL=http://192.168.3.18:8000
